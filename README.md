## Scott E. Swartz Website

#### Requirements
ruby 2.3

#### Setup
- add `.env` file to project directory
- add `FORM_URL` key to env file

#### Usage
- run `bundle install`
- run `middleman serve`
- visit `localhost:4567` to see changes
